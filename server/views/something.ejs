<h1> Bomz </h1>
<script src="/socket.io/socket.io.js"></script>
<script type="text/javascript" charset="utf-8">
  var socket = io.connect('http://localhost:3000');

  var x = 10;
  setInterval(function() {
    x+=50;
    socket.emit('controllerEvent', {
      coordinates: { x: 1, y: x, f: false },
      game: 'cantdomuch'
    });
  }, 500);
</script>
